<Page x:Class="Pr14.Pages.Page7"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr14.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page7">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <TextBlock Text="Выберите сеанс" 
                   FontSize="20" 
                   Foreground="White" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- Карточка фильма с сеансами -->
        <Border Grid.Row="1" 
            Background="White" 
            Width="500" 
            Height="450"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            BorderBrush="#DDDDDD"
            BorderThickness="1">
            <StackPanel Margin="25">

                <!-- Изображение фильма -->
                <Image Source="{Binding Film.ImagePath}" 
                   Width="150" 
                   Height="200" 
                   Stretch="UniformToFill"
                   Margin="0,0,0,15"/>

                <!-- Название фильма -->
                <TextBlock Text="{Binding Film.Name}" 
                       FontSize="18" 
                       FontWeight="Bold"
                       Foreground="#333333"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,0,0,20"/>

                <!-- Заголовок сеансов -->
                <TextBlock Text="Доступные сеансы:" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Foreground="#333333"
                       Margin="0,0,0,10"/>

                <!-- Список сеансов -->
                <ItemsControl ItemsSource="{Binding sessions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F8F9FA" 
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="5"
                                Margin="0,5,0,5"
                                Padding="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0"
                                        x:Name="ButSeans" 
                                        Content="{Binding Time}" 
                                        Tag="{Binding SessionID}"
                                        Background="#3498DB"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Padding="15,8"
                                        FontSize="14"
                                        Click="ButSeans_Click"
                                        Cursor="Hand"/>

                                    <TextBlock Grid.Column="1"
                                           Text="{Binding HallName}" 
                                           FontSize="14"
                                           Foreground="#666666"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>

                                    <TextBlock Grid.Column="2"
                                           Text="{Binding Price, StringFormat={}{0:N2} ₽}" 
                                           FontSize="14"
                                           FontWeight="Bold"
                                           Foreground="#2C3E50"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Кнопка назад -->
        <Button x:Name="GoBack" 
            Content="← Назад" 
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            Margin="20,20,0,0" 
            FontSize="14" 
            Background="#E0E0E0" 
            Foreground="#333333" 
            BorderThickness="0"
            Padding="15,8"
            Click="GoBack_Click"
            Cursor="Hand"/>
    </Grid>
</Page>
