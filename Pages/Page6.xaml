<Page x:Class="Pr14.Pages.Page6"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr14.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="800"
      Title="Экран">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <TextBlock Text="Выберите места" 
                   FontSize="20" 
                   Foreground="White" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- Схема зала -->
        <Border Grid.Row="1" 
            Background="White" 
            Width="500" 
            Height="400"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            BorderBrush="#DDDDDD"
            BorderThickness="1">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Экран -->
                <Border Grid.Row="0" 
                    Background="#E0E0E0" 
                    Height="40" 
                    CornerRadius="5"
                    Margin="0,0,0,20">
                    <TextBlock Text="ЭКРАН" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           FontSize="16" 
                           FontWeight="Bold"
                           Foreground="#666666"/>
                </Border>

                <!-- Места -->
                <ScrollViewer Grid.Row="1" 
                          HorizontalScrollBarVisibility="Auto" 
                          VerticalScrollBarVisibility="Hidden">
                    <ItemsControl ItemsSource="{Binding Rows}" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <ItemsControl ItemsSource="{Binding Seats}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding Place.Seat}" 
                                                    Tag="{Binding Place.ID}" 
                                                    Width="35" 
                                                    Height="35" 
                                                    Margin="3" 
                                                    FontSize="12"
                                                    Click="Button_Click">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="#3498DB"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="BorderThickness" Value="0"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                                                    <Setter Property="Background" Value="#BDC3C7"/>
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#2980B9"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{Binding LineNumber}" 
                                           VerticalAlignment="Center" 
                                           Margin="10,0,0,0" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Foreground="#333333"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Легенда -->
                <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal" Margin="10,0">
                        <Border Width="20" Height="20" Background="#3498DB" Margin="0,0,5,0"/>
                        <TextBlock Text="Свободно" FontSize="12" VerticalAlignment="Center" Foreground="#666666"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="10,0">
                        <Border Width="20" Height="20" Background="#BDC3C7" Margin="0,0,5,0"/>
                        <TextBlock Text="Занято" FontSize="12" VerticalAlignment="Center" Foreground="#666666"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Кнопка назад -->
        <Button x:Name="GoBack" 
            Content="← Назад" 
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            Margin="20,20,0,0" 
            FontSize="14" 
            Background="#E0E0E0" 
            Foreground="#333333" 
            BorderThickness="0"
            Padding="15,8"
            Click="GoBack_Click"
            Cursor="Hand" RenderTransformOrigin="0.507,-0.889"/>
    </Grid>
</Page>
